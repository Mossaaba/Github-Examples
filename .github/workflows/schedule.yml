name: Experiment
on: 
  schedule:
  - cron: '*/5 * * * *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "hellou mouss The current server time is $(date)"